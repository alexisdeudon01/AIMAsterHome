# AIMAsterHome

Home Assistant OS add-on repository containing `ai_dashboard_builder`, a simple add-on that generates Lovelace YAML dashboards from your real Home Assistant context using Ollama first and Anthropic as fallback.
